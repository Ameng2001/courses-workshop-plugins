---
description: Design a thematic region activity
argument-hint: [workspace]
---

Design a region activity for the specified project workspace. Pass the workspace as `$ARGUMENTS`.

Use skill: "region-activity"
