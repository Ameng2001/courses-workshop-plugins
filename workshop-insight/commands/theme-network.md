---
description: Generate a thematic curriculum theme network / 主题网络图
argument-hint: [workspace-or-theme]
---

Generate a core-theme to subtheme to domain-goal network for a thematic curriculum package. Pass the workspace or theme as `$ARGUMENTS`.

Use skill: "theme-network"
