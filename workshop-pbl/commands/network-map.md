---
description: Build a thematic network diagram from a driving question
argument-hint: [workspace-name]
---

Build a network map for the specified workspace. Pass the workspace name as `$ARGUMENTS`.

Use skill: "network-map"
