---
description: Compile all artifacts into a complete PBL proposal document
argument-hint: [workspace-name]
---

Generate the complete PBL proposal for the specified workspace. Pass the workspace name as `$ARGUMENTS`.

Use skill: "proposal-generate"
